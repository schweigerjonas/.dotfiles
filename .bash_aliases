# NeoVim standard vim editor
alias vim="nvim"
alias vi="nvim"

# Ask for confirmation before deleting multiple (> 4) files
alias rm="rm -I"

# Arduino CLI
alias ard="arduino-cli"

# tmux
alias tn="tmux new-session -s"
alias tl="tmux list-sessions"
alias ta="tmux attach-session"
